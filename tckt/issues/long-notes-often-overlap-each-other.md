# long notes often overlap each other

- i.e. notes with longer text in their body
- we want to fix this *cleanly*, without random guesses like "no way text is longer than x" and w/o introducing scrollbars