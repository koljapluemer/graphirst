# list of recently interacted w/ nodes

Make the (right) sidebar an internal tab view, using DaisyUI patterns. Make sure not to use the deprecated daisy pattern, but the current one. Web search if needed.
For now, the first tab should be the search, such as it is now. 
The second tab should be a similar list, but for recently files.
It should contain a dropdown at top, letting the user choose between created/updated/opened, corresponding to the different time stamp props of the node objects

Use a clean state machine.
Feel free to refactor; aim for clean code without god objects or giant files.