# Right Click - "Open Note Here" feature

- when right-clicking, open a mini text input w/ dropdown, which is working like the search sidebar, to search for an existing note to open there
- escape should close
- reuse search utils as much as possible

(point: we may want to iteratively go through notes in the search, so having to delete the search query to find a note is awkward)